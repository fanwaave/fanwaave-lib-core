# fanwaave-lib-core

ORM and migration source of truth for Fan and push-notification delivery across web, mobile, and desktop. Web servers may use the read-only feature; only this crate (and declarative-migrations) apply writes/migrations.
